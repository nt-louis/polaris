# Token generated in the Dockhand dashboard UI under Settings > Hawser / Environments -> Remote Host (Hawser)
HAWSER_TOKEN=your_token_here

# Optional host-network bind address. The service defaults to the VPS Tailnet address.
HAWSER_BIND_ADDRESS=${VPS_B_TAILSCALE_IP}
