# PocketID Configuration Template
# Generate a 32-byte encryption key using: openssl rand -base64 32
ENCRYPTION_KEY=your_base64_encoded_32_byte_encryption_key

# Public URL of the auth portal
APP_URL=https://auth.example.com

# Trust reverse proxy headers
TRUST_PROXY=true
