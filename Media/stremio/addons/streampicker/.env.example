# REQUIRED — the unguessable path segment gating addon installation.
# Generate: openssl rand -hex 24
ADDON_SECRET=

# Where players reach this addon from OUTSIDE the container.
ADDON_PUBLIC_URL=https://streampicker.example.com
